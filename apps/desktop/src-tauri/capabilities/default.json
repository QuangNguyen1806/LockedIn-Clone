{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main control center and overlay windows.",
  "windows": ["main", "overlay"],
  "permissions": [
    "core:default",
    "store:default",
    "allow-window-commands"
  ]
}
